// obj_player draw

draw_self();

//if (slap)  { draw_text(x, y-sprite_height, "slap!");  }
//if (trip)  { draw_text(x, y-sprite_height, "trip!");  }
//if (push)  { draw_text(x, y-sprite_height, "push!");  }
//if (block) { draw_text(x, y-sprite_height, "block!"); }

// if blocking, no damage taken everrrrrr
// physics - players should move upon being hit
// hp bar labels w name
